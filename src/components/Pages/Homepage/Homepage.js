import React from 'react';
import Slider from './Carousel/Carousel';

const Homepage = () => {
    return (
        <div>
            <Slider></Slider>
        </div>
    );
};

export default Homepage;